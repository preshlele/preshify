export enum Role {
    Customer = 'customer',
    Admin = 'admin'
}

export enum Provider {
    Google = 'google',
    Credentials = 'credentials'
}

export enum Gender {
    Male = 'male',
    Female = 'female'
}